[project]
name = "divan"
version = "0.1.0"
description = "Workspace template repository — standards, scaffolding, and project initialization tooling"
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"
dependencies = []

[project.scripts]
divan-init = "tools.init_project:main"
divan-validate = "tools.validate_template:main"
