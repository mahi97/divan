# Minimal Profile - Core essentials only
name: minimal
description: "Lightweight. Just the core plugins for any project."
icon: "--"

tags: [core]
extras: []

settings:
  model: "opus[1m]"

claude_md:
  append: |
    ## Divan Profile: minimal
    Core plugins only: superpowers, github, commit-commands, context7, ralph-loop, remember, claude-skill-manager.
    CSM runs as a persistent daemon managing your skill/plugin ecosystem. Start with `csm start`.
    Run `divan add <plugin>` to add more as needed.
