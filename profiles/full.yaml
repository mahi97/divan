# Full Profile - Everything
name: full
description: "The complete stack. Every plugin, skill, hook, and agent."
icon: "**"

# Include all tags
tags: [core, git, code-quality, workflow, frontend, backend, ai-ml, platform, code-intel, devtools, communication, lsp, session, safety, python, cpp, automation]

# No extras needed - tags cover everything
extras: []

# Project-level settings
settings:
  model: "opus[1m]"

# CLAUDE.md sections to generate
claude_md:
  append: |
    ## Divan Profile: full
    All plugins and tools are available. See `divan list` for the full inventory.
